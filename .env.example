# FlowSign nao requer variaveis de ambiente para rodar.
#
# A configuracao do Firebase (Auth, Firestore, Storage) fica em
# src/firebase-applet-config.json. As chaves web do Firebase sao
# publicas por design; a seguranca e feita pelas regras em
# firestore.rules e pelos dominios autorizados no Console do Firebase.
